//Creates an object that contains all of the elements that we select from the DOM
export const elements = {
    searchInput : document.querySelector('.search__field'),
    searchForm : document.querySelector('.search')
}